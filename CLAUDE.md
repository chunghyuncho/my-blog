# my-blog

마크다운 파일을 읽어 정적 블로그 웹사이트로 변환하는 프로젝트.

## 핵심 원칙

- **프레임워크 없음.** React, Vue, Svelte, Next.js 등 일체 사용 금지. 순수 HTML, CSS, JavaScript(바닐라 JS)로만 구현한다.
- **번들러/트랜스파일러 없음.** Webpack, Vite, Babel 등 빌드 도구를 도입하지 않는다. 브라우저가 그대로 실행할 수 있는 코드를 작성한다.
- 외부 라이브러리가 꼭 필요한 경우(예: 마크다운 파서)에만 CDN 스크립트 태그로 최소한으로 사용하고, 가능하면 직접 구현한다.
- 정적 파일만으로 동작해야 한다. 서버 사이드 로직(Node.js 서버, DB 등)이 필요 없는 구조를 지향한다.

## 디자인 원칙

- **깔끔하고 읽기 좋은 디자인.** 본문 가독성이 최우선 — 적절한 줄 간격, 본문 폭 제한(약 65-75자), 여백을 넉넉히 둔다.
- **다크 모드 지원 필수.**
  - `prefers-color-scheme` 미디어 쿼리로 시스템 설정을 기본으로 따르되, 사용자가 수동으로 토글할 수 있는 버튼 제공.
  - 선택한 테마는 `localStorage`에 저장해 재방문 시 유지.
  - 색상은 CSS 커스텀 프로퍼티(`:root`, `[data-theme="dark"]`)로 관리해 라이트/다크 값을 한 곳에서 관리한다.
- **모바일 반응형 필수.** 뷰포트 폭이 좁아져도(최소 ~360px) 가로 스크롤 없이 자연스럽게 스택되도록 구성. 미디어 쿼리 또는 유동적인 CSS(Grid/Flexbox, `clamp()`, 상대 단위)로 대응.
- 폰트, 코드 블록, 인용구, 표, 이미지 등 마크다운에서 흔히 나오는 요소들이 모두 스타일링되어야 한다.

## 기능 요구사항

- `posts/` (또는 유사한) 디렉토리에 있는 마크다운 파일들을 읽어 블로그 글 목록과 상세 페이지로 렌더링한다.
- 마크다운의 프론트매터(제목, 날짜, 태그 등 YAML 메타데이터)를 지원할지, 파일명/최상단 헤딩만으로 처리할지는 구현 시 결정 — 프론트매터 지원을 기본으로 고려한다.
- 목록 페이지: 글 제목, 날짜, (있다면) 요약/태그를 보여준다.
- 상세 페이지: 마크다운 → HTML 변환 결과를 렌더링한다.

## 아키텍처 결정 시 고려사항

마크다운 → HTML 변환을 **언제** 수행할지가 핵심 설계 결정이다:

1. **빌드 타임 변환(정적 생성)**: Node.js 스크립트로 마크다운을 미리 HTML로 변환해 정적 파일을 생성. 별도 빌드 스크립트가 필요하지만 배포 후에는 순수 정적 파일만 서빙하면 되고 SEO/로딩 속도에 유리하다.
2. **런타임(클라이언트) 변환**: 브라우저에서 `fetch`로 `.md` 파일을 읽어 JS로 즉시 파싱해 렌더링. 빌드 단계가 없어 더 단순하지만, 초기 로딩 시 마크다운 파싱 비용과 `file://`로 열었을 때의 CORS 제약을 감안해야 한다(로컬 개발 서버 필요).

프로젝트 구조나 코드가 아직 없으므로, 실제 구현을 시작할 때 사용자와 이 중 하나를 확정한다. 특별한 언급이 없다면 빌드 없이 바로 열어볼 수 있는 **런타임 변환 + 로컬 정적 서버**를 기본값으로 제안한다.

## 코딩 컨벤션

- 파일은 용도별로 분리: `index.html`(글 목록), `post.html`(글 상세), `css/style.css`, `js/main.js` 등 — 지나친 세분화는 피한다.
- 주석은 최소화하고, 정말 비직관적인 이유가 있을 때만 짧게 남긴다.
- 접근성(a11y) 기본기를 지킨다: 시맨틱 태그, 적절한 `alt`, 다크모드 토글 버튼에 `aria-label`.
